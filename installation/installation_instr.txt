# For base epymarl with gym support
# git clone https://github.com/uoe-agents/epymarl.git
cd epymarl/installation
conda create -n epymarl python=3.8.18 -y && \
conda activate epymarl && \
pip install wheel==0.38.4 setuptools==65.5.0 einops && \
pip install -r requirements.txt && \
sh ./install_torch_scatter.sh

# For SMAC
bash install_sc2.sh

# For LBF
pip install lbforaging

# For RWARE
pip install rware

# For installing matrix-games
pip install git+https://github.com/semitable/matrix-games.git

# For installing and compatibility with MPE
pip install seaborn git+https://github.com/semitable/multiagent-particle-envs.git

# For MARBLER (assumming already in "epymarl/installation" directory)
bash install_MARBLER.sh

### For pettingzoo

## Prerequisites
pip install opencv-python-headless==4.9.0.80
# or
pip install opencv-python==4.9.0.80 # only for rendering

## All (of our interest: Atari, Butterfly)
pip install transformers==4.38.2 pettingzoo==1.24.3 'pettingzoo[atari]'==1.24.3 autorom==0.6.1 'pettingzoo[butterfly]'==1.24.3

## or, individual
# Atari
pip install 'pettingzoo[atari]'==1.24.3
pip install autorom==0.6.1
AutoROM -y
# Butterfly
pip install 'pettingzoo[butterfly]'==1.24.3
# Classic
pip install 'pettingzoo[classic]'==1.24.3
# MPE
pip install 'pettingzoo[mpe]'==1.24.3
# SISL
sudo apt-get install swig
pip install 'pettingzoo[sisl]'==1.24.3

